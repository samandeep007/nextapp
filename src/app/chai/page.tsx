import React from 'react'

// In typescript, the folder can have any name, but the files must be named page.tsx
//layout.tsx file in the src/app folder is the layout component that is used to wrap all the pages in the app. It contains the metadata and the layout of the app.

export default function ChaiPage() {
  return (
    <div className='text-3xl'>chai page</div>
  )
}
